PORT=4000
GEMINI_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
# Optional. If set, /api/ingest and /api/query require a matching
# x-api-key header. Leave blank for open local development.
API_KEY=
# Comma-separated list of allowed CORS origins. Defaults to the local
# Vite dev server if unset.
FRONTEND_URL=http://localhost:5173
